logging:
  config: classpath:logback-production.xml
  
eureka:
  instance:
    hostname: pilipa-eureka
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/